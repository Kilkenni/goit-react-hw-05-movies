export default function matchGenresByID(genreIdArray, genreDefinitionArray) {

  //turns out now TMDB returns genres with names => not needed

  /*
  structure of genreIdArray from TMDB: 
  [ {id: ID, name: NAME}, ...  ]
  */
  
  /*
  structure of genreDefinitionArray:

  */
}